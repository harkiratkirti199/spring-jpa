server:
  port: 8085
  servlet:
    register-default-servlet: true
spring:
  datasource:
    url: jdbc:h2:mem:appdb
    username: sa
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
debug: true

